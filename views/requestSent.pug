extends layout

mixin message(opp, name)
    if(opp)
        h1.greeting Your request has been sent.
    else 
        h1.greeting Your request has been sent.

append links 
    li(style="float: right;")
        a(href="/loggout") Loggout 
    
block content
    .dashboard 
        .user 
            +message(opp, name)

    form(method="post" action="/requestSent")
        table 
            tr 
                td
                td
                    input(type="submit" value="OK")